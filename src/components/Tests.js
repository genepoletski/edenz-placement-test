import React from 'react';

const Tests = props => {
  return (
    <div>

      <button
        className="pure-u-1 pure-button pure-button-primary">
        Grammar Test
      </button>

      <button
        className="pure-u-1 pure-button pure-button-primary">
        Listening Test
      </button>

    </div>
  );
}

export default Tests;